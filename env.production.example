# Production Environment Variables
NODE_ENV="production"

# Database URL (PostgreSQL pour la production)
# Remplacez par votre URL PostgreSQL de production
DATABASE_URL="postgresql://coachia_user:password@localhost:5432/coachia"

# JWT Secret (générez une clé sécurisée)
JWT_SECRET="votre_secret_jwt_super_securise_pour_production"

# Next.js
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-nextauth-secret"

# Email (optionnel)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# API Keys (optionnel)
OPENAI_API_KEY="votre_cle_api_openai" 